export type TestimonialItem = {
    img: { width: number, height: number, src: string },
    quote: string,
    name: string,
    role: string
};

export type ClientSliderItem = {
    src: string
    name: string
    height: number
    width: number
}